import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    borderWidth: 2,
    borderRadius: 8,
    padding: 10,
    gap: 8,
    height: 150,
    width: "95%",
  },

  header: {
    fontWeight: 600,
    fontSize: 20,
  },

  paragraph: {},
});

export default styles;
